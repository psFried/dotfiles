[default]
# v2 uses the pager for every. single. thing. ugh, just disable it I guess
cli_pager=
# V2 treats all binary data arguments as if they were _already_ base64 encoded, and silently ignores
# any thing from outside the base64 character set. What a turd.
cli_binary_format=raw-in-base64-out
region = us-east-1
output = json
